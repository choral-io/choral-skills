# Local-only Knowledge Workflow execution context.
# This file is optional and must stay SCM-ignored.
#
# Currently supported setting:
#
# current_member:
#     member_id: Gavroche
#
# Use this only when the local Git identity is not the repository member id.
# The member_id value must exactly match knowledge/members/<member-id>.md.
