interface:
  display_name: "Delivery Review"
  short_description: "Review delivery work before completion"
  default_prompt: "Use $delivery-review to validate this delivery card before completion."
policy:
  allow_implicit_invocation: true
